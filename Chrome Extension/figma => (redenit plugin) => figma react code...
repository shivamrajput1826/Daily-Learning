figma => (redenit plugin) => figma react code....
header => common(4-5) => supervised learning
kyc bundle => storybook 
figma=> margin,padding,
1) icons present or not => check => google lens...( automatic url generate)=>browser(storybook)
2) component present or not (combination of components)

json
Image => url => icon present hai nhi
icon present api=> 3 icons=> end points
 
1)icon pick manually
2)dashboard=> icon => check button => api call to storybook=>check 
                  -> svg utha le and search svg
                  or supervised learning 
                  ->
                  ->
                  ->
                  ->
   ->present=> url => 
   ->no present => manually add(command)


image => storybook ke images compare krna hoga(svg, png)